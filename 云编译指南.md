# 云编译指南：本机零安装，拿回 2–3 MB 成品

## 原理

```
你的源码  ──推送──▶  GitHub 仓库  ──自动触发──▶  Actions 的 Windows runner 编译
                                                      │
        ◀────────── 下载 2–3 MB 成品 ◀──────  Artifacts ──┘
```

GitHub 的 `windows-latest` runner **已经预装好** VS2022 Enterprise（含"使用 C++ 的桌面开发"
工作负载）和 Windows SDK 10.0.22621 / 10.0.26100。所以**编译这件事在别人机器上做**，
你本地一个字节的工具链都不用装。

---

## 准备工作

只需要一个 GitHub 账号（免费注册：https://github.com/signup ）。

本地这边我已经全部弄好了——`TaskbarEqWidth\` 已经是一个 git 仓库：

```
63d69c0  TaskbarEqWidth v0.1: Win11 任务栏按钮等宽（支持干净卸载）
7888010  加入 .gitattributes：强制 build.bat 使用 CRLF
```

---

## 路线 A：用 git 推送（推荐，约 5 分钟）

### 第 1 步：建一个空仓库

1. 打开 https://github.com/new
2. **Repository name** 填 `TaskbarEqWidth`
3. 可见性选 **Public**（Actions 完全免费、不限时长）；想选 Private 也行（每月 2000 分钟
   免费额度，这个项目每次编译约 3 分钟，够用）
4. **不要**勾选 "Add a README file" / ".gitignore" / "license" —— 保持空仓库
5. 点 **Create repository**

### 第 2 步：推送

打开**命令提示符**（Win+R 输入 `cmd`），逐行执行，把 `你的用户名` 换成你的 GitHub 用户名：

```bat
set GIT=C:\Users\Lenovo\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd\git.exe
cd /d C:\Users\Lenovo\WorkBuddy\2026-09-12-17-12-38\TaskbarEqWidth
%GIT% remote add origin https://github.com/你的用户名/TaskbarEqWidth.git
%GIT% push -u origin main
```

推送时**会自动弹出一个浏览器窗口**让你登录 GitHub 并授权（这是 git 自带的凭据管理器，
不需要你手动生成 token）。授权一次，以后就不用再登了。

### 第 3 步：等编译

推送成功后打开：

```
https://github.com/你的用户名/TaskbarEqWidth/actions
```

会看到一条名为 **Build TaskbarEqWidth (x64)** 的运行记录，黄色圆点 = 正在编译，
绿色对勾 = 成功。**大约 2–4 分钟。**

---

## 路线 B：完全不用 git（纯浏览器，约 8 分钟）

如果你不想碰命令行：

1. 同样先建一个空仓库（同路线 A 第 1 步）
2. 在空仓库页面点 **uploading an existing file** 链接
3. 打开本地文件夹 `C:\Users\Lenovo\WorkBuddy\2026-09-12-17-12-38\TaskbarEqWidth`，
   把 **`src` 文件夹**、**`third_party` 文件夹**、以及 `README.md`、`build.bat`、
   `.gitattributes`、`.gitignore` 一起拖进浏览器窗口，点 **Commit changes**
   - 提示：`.gitattributes` / `.gitignore` 是隐藏文件，需要先在资源管理器里
     「查看 → 显示 → 隐藏的项目」才能看到
4. 建编译配置文件：点 **Add file → Create new file**，在文件名框里输入
   `.github/workflows/build.yml`（输入斜杠会自动建目录），把本地
   `.github/workflows/build.yml` 的内容整个粘贴进去，点 **Commit changes**

提交后 Actions 会自动开始跑（因为配置里写了 `on: push`）。

---

## 第 4 步：下载成品

打开 https://github.com/你的用户名/TaskbarEqWidth/actions ，点进那条运行记录，
**拉到页面最底部**，在 **Artifacts** 区域能看到：

```
TaskbarEqWidth-x64        ← 点它下载，得到一个 zip
```

解压后是**两个文件，必须放在同一个文件夹里**：

| 文件 | 用途 |
|---|---|
| `TaskbarEqWidth.exe` | 管理器：安装 / 卸载 / 状态 |
| `TaskbarEqWidthHook.dll` | 注入到 explorer 的钩子 |

Artifacts 默认保留 90 天，过期后重新 push 一次就会有新的。

---

## 第 5 步：使用

**以管理员身份**打开命令提示符（开始菜单搜 cmd → 右键 → 以管理员身份运行）：

```bat
cd /d 你解压出来的文件夹
TaskbarEqWidth.exe                  :: 安装并立即生效（默认宽度 176）
TaskbarEqWidth.exe --status         :: 查看状态
TaskbarEqWidth.exe --width 200      :: 改成别的宽度
TaskbarEqWidth.exe --uninstall      :: 完全卸载
```

### 怎么判断成功

```
TaskbarEqWidth.exe --status
```

```
DLL 是否驻留 explorer : 是（正在生效）
钩子状态             : 已挂上          ← 关键就是这一行
当前按钮宽度         : 176
```

看到 **"已挂上"** 才算成功。

### 如果显示"未挂上"

按可能性排序：

1. **被杀毒软件拦了**（最常见）。把安装目录加白名单后重试。这不是代码有问题，
   向 explorer 注入 DLL 本身就属于敏感动作，火绒 / 360 / Defender 大概率会拦。
2. **首次运行网络不通**——第一次需要从微软符号服务器下载 PDB（几 MB），
   检查安装目录下 `symbols\Taskbar.View.pdb` 是否生成了。
3. **Windows 版本不被支持**——检查 `--status` 输出，把结果发我。

---

## 改了代码之后

我改完代码你只要再推一次：

```bat
cd /d C:\Users\Lenovo\WorkBuddy\2026-09-12-17-12-38\TaskbarEqWidth
%GIT% add -A
%GIT% commit -m "改动说明"
%GIT% push
```

Actions 会自动重新编译，几十秒后新的 artifact 就能下载了。

---

## 编译失败怎么办

把 Actions 页面里**失败那一步的完整日志**复制给我。日志里会先打印编译环境信息
（VS 路径、SDK 版本、C++/WinRT 头文件是否存在），定位问题很快。
